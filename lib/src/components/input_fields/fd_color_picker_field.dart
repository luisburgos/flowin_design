import 'package:flowin_design/flowin_design.dart';
import 'package:flutter/material.dart';

/// @no-doc
class FDColorPickerField extends StatelessWidget {
  /// @no-doc
  const FDColorPickerField({super.key});

  @override
  Widget build(BuildContext context) {
    return const FDInputField(
      label: 'Color',
      child: Center(
        child: Text('TODO'),
      ),
    );
  }
}
